# requirements-ci.txt — Dependency ringan khusus untuk GitHub Actions CI/CD
# Hanya ada yang WAJIB untuk: test + train + evaluate + register

# Core ML
scikit-learn>=1.4.0
xgboost>=2.0.0
pandas>=2.1.0
numpy>=1.26.0

# Imbalanced data (untuk SMOTETomek)
imbalanced-learn>=0.12.0

# MLOps (tracking + registry)
mlflow>=2.10.0

# Config parsing
pyyaml>=6.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
